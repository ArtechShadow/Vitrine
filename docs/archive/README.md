# Archived documentation

Legacy material retained for provenance, no longer part of the active docs.

- `Python_API_issues.md`, `Python_UI.md`, `imgui_inventory.md` — internals of the LichtFeld
  Studio ImGui application. Vitrine now **vendors** LichtFeld as a pinned tool (submodule
  @ v0.5.3) and does not develop its UI, so these are historical reference only.
